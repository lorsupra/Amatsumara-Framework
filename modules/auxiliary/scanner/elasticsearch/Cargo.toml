[package]
name = "elasticsearch_scanner"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
amatsumara-api = { path = "../../../../amatsumara-api" }
tokio = { version = "1", features = ["full"] }
