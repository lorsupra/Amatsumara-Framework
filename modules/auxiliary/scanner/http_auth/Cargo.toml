[package]
name = "http_auth_brute"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
amatsumara-api = { path = "../../../../amatsumara-api" }
reqwest = { version = "0.11", features = ["blocking"] }
base64 = "0.21"
