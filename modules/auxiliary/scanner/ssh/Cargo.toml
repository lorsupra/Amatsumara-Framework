[package]
name = "ssh_login_scanner"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
amatsumara-api = { path = "../../../../amatsumara-api" }
serde_json = "1.0"
tokio = { version = "1", features = ["full"] }
